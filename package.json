{
  "name": "demo-sales",
  "version": "1.0.0",
  "description": "Demo Sales Inc. â€” Authorized Anheuser-Busch Beverage Distributor, El Dorado KS",
  "scripts": {
    "build": "node build.js",
    "deploy:staging": "node build.js && npx wrangler pages deploy ./dist --project-name demosales --branch staging",
    "deploy:prod": "node build.js && npx wrangler pages deploy ./dist --project-name demosales --branch main"
  },
  "private": true
}


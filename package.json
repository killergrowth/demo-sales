{
  "name": "demo-sales",
  "version": "1.0.0",
  "description": "Demo Sales Inc. — Authorized Anheuser-Busch Beverage Distributor, El Dorado KS",
  "scripts": {
    "build": "node build.js",
    "deploy:staging": "node build.js && npx wrangler pages deploy ./dist --project-name demo-sales --branch staging",
    "deploy:prod": "node build.js && npx wrangler pages deploy ./dist --project-name demo-sales --branch main"
  },
  "private": true
}
